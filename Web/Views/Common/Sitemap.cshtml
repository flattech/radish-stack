@model Web.Models.Common.SitemapModel
@{
    //title
    Html.AddTitleParts(T("PageTitle.Sitemap").Text);
}
<div class="page sitemap-page">
    <div class="page-title">
        <h1>@T("Sitemap")</h1>
    </div>
    <div class="page-body">
        <div class="description">
            @T("Sitemap.Description")
        </div>
        <div class="entity">
            <div class="entity-title">
                <h2>@T("Sitemap.General")</h2>
            </div>
            <div class="entity-body">
                <ul>
                    <li><a href="@Url.RouteUrl("HomePage")">@T("HomePage")</a></li>
                    @*<li><a href="@Url.RouteUrl("ProductSearch")">@T("Search")</a></li>
                    <li><a href="@Url.RouteUrl("ContactUs")">@T("ContactUs")</a></li>*@
                    @*@if (Model.Topics.Count > 0)
                    {
                        foreach (var item in Model.Topics)
                        {
                            <li><a href="@Url.RouteUrl("Topic", new {Sename = Html.GetTopicSeName(item.SystemName)})">@item.Title</a></li>
                        }
                    }*@
                </ul>
            </div>
        </div>
        @if (Model.Categories.Count > 0)
        {
            <div class="entity">
                <div class="entity-title">
                    <h2>@T("Sitemap.Categories")</h2>
                </div>
                <div class="entity-body">
                    <ul>
                        @foreach (var category in Model.Categories)
                        { 
                            <li><a href="@Url.RouteUrl("Term", new { SeName = category.SeName })">@category.Name</a></li>
                        }
                    </ul>
                </div>
            </div>
        }
      
        @if (Model.Products.Count > 0)
        {
            <div class="entity">
                <div class="entity-title">
                    <h2>@T("Sitemap.Products")</h2>
                </div>
                <div class="entity-body">
                    <ul>
                        @foreach (var product in Model.Products)
                        { 
                            <li><a href="@Url.RouteUrl("Post", new { SeName = product.Title })">@product.Title</a></li>
                        }
                    </ul>
                </div>
            </div>
        }
    </div>
</div>
